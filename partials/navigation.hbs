      <ul id="menu-menu" class="menu">
        {{#foreach navigation}}
        <li id="menu-item-{{slug}}" class="menu-item"><a href="{{url absolute="true"}}">{{label}}</a></li>
        {{/foreach}}
      </ul>
